position_source:                            # static source outputs the same coordinates twice a second
  type: static
  lat: 52
  lon: 10
  # read_interval_s: 0.5                    # how often a new static position message should be emitted
# position_source:                          # command source tries to read NMEA sentences from stdout of given command
#   type: command
#   command: ["gpspipe", "-r"]              # this is passed to Popen; if you want to use a bash pipeline do ["bash", "-c", "command | command2"]
#   read_timeout_s: 2                       # how long until gps is considered to be non available


# gps_filter:                               # enable and configure alpha-beta-filter for GPS data smoothing
#   enabled: true
#   alpha: 0.7
#   beta: 0.04

log_level: INFO
redis:
  host: valkey
  port: 6379
  stream_id: self
  output_stream_prefix: positionsource

prometheus_port: 8006
